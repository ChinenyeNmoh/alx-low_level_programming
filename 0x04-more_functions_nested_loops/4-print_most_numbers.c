#include "main.h"

/**
 * print_most_numbers - prints number
 *
 * Return: void
 */

void print_most_numbers(void)
{
	int i
		for (i = 0; i <= 9; i++)
		{
			if ((c != 2) && (c != 4))
			{
				_putchar('0' + i);
			}
			_putchar('\n');
		}
}
